# Delivery Evidence Matrix

This matrix maps the delivery requirements for the lidar wrong-way prevention dashboard to the automated and field evidence that should be attached to the acceptance package.

| Requirement Area | Delivery Requirement | Automated Evidence | Field Evidence Still Required |
| --- | --- | --- | --- |
| Requirements | Field context, lidar payload rules, control-board TCP assumptions, and known unresolved field decisions are documented. | `docs/ai/field-system-requirements.md`, `docs/specs/lidar-dashboard-payload.md`, `docs/specs/dashboard-control-board-protocol.md`, `npm run verify:env-contracts` | Final control-board IP/port, heartbeat/ack criteria, and level-2 escalation thresholds from field measurement |
| API And Swagger | Operator, ingest, event, statistics, health, database health, device, status, and control-board APIs are documented with auth and device-key contracts. | `npm run verify:swagger-contracts`, `GET /api-docs.json`, `GET /api/health`, authenticated `GET /api/database/health`, `docs/ops/acceptance-checklist.md` | Swagger access policy confirmed through the field Nginx allowlist |
| DB And Prisma | Vehicle tracks, traffic events, command logs, device status logs, sites, zones, and devices are represented in Prisma and seeded/migrated. | `npm run verify:prisma-contracts`, `npm run verify:db-field-rehearsal`, `npm run ci:db`, `npm run db:status`, `scripts/db-field-rehearsal.ps1`, authenticated `GET /api/database/health`, authenticated `GET /api/status` | Field seed data reviewed for actual site, zone, and device names |
| Lidar Ingest | `normal-driving` updates unique vehicle tracks without duplicate events; wrong-way stages create/reuse events; raw payload is retained. | `npm run verify:wrongway-contracts`, `npm run verify:lidar-rehearsal`, `scripts/runtime-smoke.ps1`, `scripts/lidar-ingest-rehearsal.ps1`, `/api/events/summary` `vehiclesPassed` | Lidar PC sends representative JSON over the internal network |
| Control Board TCP | Dashboard sends raw 10-byte TCP command frames with CRC-8 vectors, dry-run default, explicit live approval gating, distinct connect/response timeout handling, per-attempt retry logging, response parsing, ACK latency fields, authenticated manual command audit metadata, and clear separation between the primary TCP command path, HTTP bridge diagnostics, and legacy serial alias. | `npm run verify:control-board-protocol`, `npm run verify:control-board-tcp`, `npm run verify:control-board-latency`, `npm run verify:manual-control-command`, `npm run verify:control-board-field-rehearsal`, `npm run verify:swagger-contracts`, `scripts/control-board-field-rehearsal.ps1`, `/api/ingest/control-board/tcp/test`, `GET /api/control-board/status`; manual commands record `requestedByUserId`, `MANUAL_TEST`, `LIVE_TCP_APPROVAL_REQUIRED`, `TCP_SEND_ATTEMPT_FAILED`, `averageResponseMs`, `responseSampleCount`, `liveApproved`, and `responseDurationMs`; Swagger documents `/api/ingest/control-board` as HTTP bridge diagnostics and `/api/ingest/control-board/serial/test` as a legacy alias. | Live integrated control-board TCP test with field IP/port, `CONTROL_BOARD_LIVE_APPROVED=true`, and hardware approval |
| Frontend Control UI | Operator UI shows current status, event detail, raw payload, linked commands, packet hex, device status, WebSocket connected/degraded/disabled state, and DRY_RUN/LIVE_TCP state. | `npm run verify:frontend-ui-contracts`, `npm run verify:frontend-settings-contracts`, `npm run verify:realtime-contracts`, `npm --prefix dashboard/dashboard-web run lint`, `npm run build:web`, `scripts/field-acceptance.ps1 -OperatorUiWalkthroughEvidence <path>` | Browser walkthrough on the delivery display resolution attached to the field acceptance manifest |
| Traffic Statistics | Daily, weekly, monthly, and yearly statistics use DB unique track counts, wrong-way counts, wrong-way rate, command metrics, and TCP ACK average response metrics. API range enum values are `daily`, `weekly`, `monthly`, and `yearly`. | `npm run verify:statistics-metrics` checks executable KPI vectors for unique track counts, wrong-way rate, DRY_RUN/LIVE command separation, ACK/FAILED success rate, and average TCP ACK response time; `npm run verify:statistics-contracts` checks backend range modes, Swagger enum, dashboard segmented control, DB unique track counts, wrong-way rate, command metrics, and `averageResponseMs`; `scripts/runtime-smoke.ps1` checks `GET /api/statistics/traffic?range=daily`. | Field acceptance of period labels and operational KPI wording |
| Authentication | JWT login uses HttpOnly access cookies, readable CSRF cookie for mutations, Bearer compatibility for scripts, logout cookie clearing, and Secure cookies whenever `SameSite=None` is selected. | `npm run verify:auth-cookie`, `npm run verify:auth-crypto`, `scripts/runtime-smoke.ps1` auth checks | HTTPS/TLS topology confirms `AUTH_COOKIE_SECURE` and `AUTH_COOKIE_SAMESITE` values |
| Nginx And Runtime | Nginx proxies UI/API/WebSocket/Swagger, applies security headers, rate-limits wrong-way ingest with HTTP 429 on limit hits, sets SPA/asset cache policy, and exposes runtime smoke coverage for statistics/control-board status APIs. | `npm run verify:delivery-proxy-contracts`, `docker compose config --quiet`, `npm run runtime:evidence`, `scripts/runtime-smoke.ps1` header/API checks for statistics and control-board status | Runtime smoke against the delivery PC Nginx entrypoint |
| Security | Dependency audit policy, secret/container/ZAP evidence paths, skipped-check reasons, tool inventory, acceptance classifications, and active-scan restrictions are documented. | `npm run verify:audit-policy`, `npm run security:evidence`, `scripts/security-scan.ps1` | Optional gitleaks, Trivy image scan, and ZAP baseline outputs when tools are installed |
| Delivery Evidence | Build, lint, smoke, server tests, audit policy, Docker compose config, runtime evidence, security evidence, field preflight, ordered field acceptance, final completion audit, handover index, closure plan, readiness report, field risk register, field action board, field gate closure map, field owner briefs, handover package, manual evidence drafts, manual evidence readiness, and field limitations are packaged with raw logs; completion audit and closure plan record redacted required field value states and required manual evidence status; the risk register turns open field/security/manual gates into reviewer-copyable acceptance draft rows without replacing signed evidence; the action board groups remaining final-status gates by owner, priority, command, evidence, and close criteria; the gate closure map groups those gates by command with category/status counts; owner briefs split that board into per-owner markdown files for field handoff; closure plan remains `OPEN` while completion blockers, field readiness open checks, open required field values, field rehearsal follow-ups, or manual evidence gaps remain; unavailable rehearsal replacement owner and target recheck date are surfaced through `Field Rehearsal Follow-ups`, `Field Rehearsal Follow-up Actions`, and `Field Evidence Follow-ups`; `handover:package` refreshes readiness, the risk register, the action board, the gate closure map, and owner briefs before indexing final evidence, and strict mode fails while any of those field action artifacts still have open risk/action/gate/owner counts. | `npm run field:preflight`, `npm run field:acceptance`, `npm run field:rehearsal-unavailable`, `npm run delivery:evidence`, `npm run completion:audit`, `npm run handover:index`, `npm run field:closure-plan`, `npm run manual:evidence-drafts`, `npm run manual:evidence-readiness`, `npm run field:readiness`, `npm run field:risk-register`, `npm run field:action-board`, `npm run field:gate-closure-map`, `npm run field:owner-briefs`, `npm run handover:package`, `npm run verify:delivery-evidence-summary`, `artifacts/field-preflight/<timestamp>/manifest.json`, `artifacts/field-preflight/<timestamp>/manifest.md`, `artifacts/field-acceptance/<timestamp>/manifest.json`, `artifacts/field-acceptance/<timestamp>/manifest.md`, `artifacts/manual-evidence-drafts/<timestamp>/manifest.json`, `artifacts/manual-evidence-drafts/<timestamp>/manifest.md`, `artifacts/manual-evidence-readiness/<timestamp>/manifest.json`, `artifacts/manual-evidence-readiness/<timestamp>/manifest.md`, `artifacts/delivery/<timestamp>/manifest.md`, `artifacts/delivery/<timestamp>/manifest.json`, `artifacts/delivery/<timestamp>/runtime/`, `artifacts/delivery/<timestamp>/security/`, `artifacts/completion-audit/<timestamp>/manifest.json`, `artifacts/completion-audit/<timestamp>/manifest.md`, `artifacts/handover-index/<timestamp>/manifest.json`, `artifacts/field-closure-plan/<timestamp>/manifest.json`, `artifacts/field-readiness/<timestamp>/manifest.json`, `artifacts/field-risk-register/<timestamp>/manifest.json`, `artifacts/field-action-board/<timestamp>/manifest.json`, `artifacts/field-gate-closure-map/<timestamp>/manifest.json`, `artifacts/field-owner-briefs/<timestamp>/manifest.json`, `artifacts/handover-package/<timestamp>/manifest.json`, `artifacts/manual/operator-ui-walkthrough.md`, `artifacts/manual/field-risk-acceptance.md`; summary vectors check handover summary status, coverage counts, failed command count, field readiness review/skipped counts, manual evidence open count, manual evidence missing count, manual evidence invalid count, field verification required areas, replacement-owner/recheck follow-ups, field action artifact open counts, and `canMarkGoalComplete`. | Attach generated artifacts, field preflight manifest, field acceptance orchestrator manifest, completion audit, handover index, closure plan, manual evidence draft/readiness manifests, readiness report, field risk register, field action board, field gate closure map, field owner briefs, handover package manifest, unavailable field rehearsal REVIEW manifests with replacement owner and target recheck date if used, manual operator UI/risk acceptance evidence, and live runtime smoke logs to the handover package |
| Final Status | The project is not marked complete until completion audit status is `COMPLETE`, `canMarkGoalComplete=true`, field readiness is `PASS`, field acceptance is `PASS` with `readyForHandover=true`, `requiresFieldReview=false`, zero review/skipped steps, and operator UI walkthrough `PASS`, control-board safety is `LIVE_TCP_READY`, `CONTROL_BOARD_LIVE_APPROVED=true`, manual evidence is `PRESENT`, manual evidence readiness is `READY`, a security evidence manifest exists, required scanner evidence is not `strictAcceptanceBlocked`, security disposition has zero `BLOCKING` and zero `DELIVERY_FIX` findings, field risk register status is `NO_OPEN_RISKS`, field action board, field gate closure map, and field owner briefs are `READY_TO_CLOSE` with zero open items/gates, handover package status is `READY`, all latest referenced artifacts including security evidence match, the final delivery source is clean on `dev` and pushed to `origin/dev`, Git-bearing evidence is generated from the clean final source revision, field readiness/field acceptance/security/runtime/handover evidence was generated for the same delivery Nginx base URL used by `final:status`, and no residual field gates remain; when latest artifacts exist, final status verification checks that incomplete states expose blockers and residual gates, and the final status report must say `READY_TO_CLOSE`. | `npm run verify:final-status`, `npm run final:status`, `npm run final:execution-plan`, `npm run manual:evidence-readiness`, `artifacts/final-status/<timestamp>/manifest.json`, `artifacts/final-status/<timestamp>/manifest.md`, `artifacts/final-execution-plan/<timestamp>/manifest.json`, `artifacts/final-execution-plan/<timestamp>/manifest.md`, `artifacts/manual-evidence-readiness/<timestamp>/manifest.json`, `artifacts/manual-evidence-readiness/<timestamp>/manifest.md`, `artifacts/completion-audit/<timestamp>/manifest.json`, `artifacts/field-readiness/<timestamp>/manifest.json`, `artifacts/field-acceptance/<timestamp>/manifest.json`, `artifacts/security/<timestamp>/manifest.json`, `artifacts/handover-package/<timestamp>/manifest.json`, `Residual Field Gates`, `remainingGates`, `completionBlockers`, `strictFailureReasons`, `requiredFieldValues`, `manual evidence`, `manual evidence readiness`, `strictAcceptanceBlocked`, `DELIVERY_FIX_REQUIRED`, `BLOCKING_FINDINGS`, `Field Risk Register`, `Field Action Board`, `Field Gate Closure Map`, `Field Owner Briefs`, `openRiskCount`, `openActionCount`, `openGateCount`, `openItemCount`, `latest referenced artifacts`, `Field Acceptance`, `OPERATOR_UI_REVIEW`, `Git Delivery State`, `WRONG_BRANCH`, `WRONG_UPSTREAM`, `UNPUSHED`, `Source Revision Freshness`, `Evidence Source Revision`, `Delivery Entrypoint Consistency`, `Delivery Entrypoint`, `MISMATCH`, `security evidence`, `no residual field gates`, `CONTROL_BOARD_LIVE_APPROVED=true`, `LIVE_TCP_READY`; final execution plan ordered commands include `completion:audit`, `handover:index`, `field:closure-plan`, strict `handover:package`, and `final:status` after the field/manual/security evidence commands. | Latest final status report, final execution plan, manual evidence readiness, field readiness, field acceptance, completion audit, security evidence, risk register, action board, gate closure map, owner briefs, and handover package prove `READY_TO_CLOSE`/PASS/READY/COMPLETE with fresh delivery/readiness/acceptance/security/index/closure references, zero blocking or delivery-fix security findings, zero open field risk/action/gate/owner items, matching delivery entrypoint evidence, `dev` -> `origin/dev` push state, clean source revision evidence, and no residual field gates, or explicitly list remaining field/runtime/hardware/manual/security gates and their ordered closure commands |

Field gate closure map evidence (`artifacts/field-gate-closure-map/<timestamp>/manifest.json`) groups the latest field action board by command so reviewers can see which final-status gates, owners, phases, categories, statuses, evidence paths, and close criteria each field command is expected to resolve. The field gate closure map is reviewer-facing execution evidence, not completion evidence.

## Acceptance Rule

- Every automated evidence command listed above must be either passing in `npm run delivery:verify` or explicitly captured in `npm run delivery:evidence`.
- Every field evidence item that cannot be executed on the development PC must remain visible as a field verification item rather than being reported as complete.
- Real integrated control-board live TCP tests require field IP/port values, `CONTROL_BOARD_LIVE_APPROVED=true`, and hardware approval before `CONTROL_BOARD_DRY_RUN=false`.
